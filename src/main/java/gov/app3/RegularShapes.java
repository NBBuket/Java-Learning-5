package gov.app3;

interface RegularShapes {
    double area(double x);
    double perimeter(double x);
    void objectJSON(int x, int y, int z);
    void drawingShape(int x, int y, int z);
}
